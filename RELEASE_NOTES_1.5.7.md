# Release Notes - Version 1.5.7

## 🎯 Основні зміни

### ✨ Нові функції

#### 🔁 Перевірка дублікатів (Duplicates Checker)
- **Новий модуль перевірки**: Додано `excel_duplicates.py` для виявлення повторюваних записів
- **Автоматична перевірка**: Сканує колонки:
  - **ЗС**: Колонка D (ПІБ)
  - **БЗ**: Колонка E (ПІБ)
  - **3бСпП БЗ**: Колонка E (ПІБ)
- **Детальні результати**: Показує всі рядки з дублікатами та кількість повторень
- **Інтеграція в LOG**: Результати автоматично записуються в лист LOG
- **UI контроль**: Додано чекбокс "Дублі" в налаштуваннях обробки

### 🐛 Виправлені помилки

#### ⚠️ Критичне виправлення порядку виконання
- **Проблема**: При першому запуску Mismatches та Slice_Check не показували результати
- **Причина**: Перевірки читали файл з диска до його збереження
- **Рішення**: 
  - Збереження файлу перенесено перед виконанням перевірок
  - Додано пересоздання LOG після всіх перевірок
  - Фінальне збереження з повними результатами

#### 📋 Новий порядок виконання
```
1. 3БСП обробка
2. Копіювання даних з інших файлів
3. Sanitizer - очищення даних
4. Створення LOG + Збереження файлу
5. Mismatches - перевірка невідповідностей
6. Slice_Check - перевірка "зрізів" (F/G)
7. Duplicates - перевірка дублікатів (NEW!)
8. Пересоздання LOG з усіма результатами
9. Фінальне збереження
```

### 🔧 Технічні покращення

#### Python модулі
- `excel_duplicates.py` - новий модуль для перевірки дублікатів
- `excel_processor.py` - додано метод `check_duplicates()`
- Оптимізовано порядок виконання для коректної роботи перевірок

#### Frontend
- `ExcelProcessor.ts` - додано обробку чекбокса дублікатів
- `SubunitMappingProcessor.ts` - додано `enable_duplicates` конфіг
- `index.html` - додано UI елемент "Дублі"

#### Налаштування
- Додано збереження/завантаження стану чекбокса "Дублі"
- Настройка передається в Python через конфіг
- За замовчуванням увімкнено

## 📊 Приклад виводу в LOG

### Дублікати
```
=== ПЕРЕВІРКА ДУБЛІКАТІВ ===
Операція           | Джерело    | Аркуш        | Рядків | Деталі
----------------------------------------------------------------------
Знайдено дублікатів| -          | ЗС/БЗ/3бСпП  | 3      | Повторюються значення...
Дублікат          | ЗС         | Колонка D    | 2      | «Іванов І.І.» — рядки: 5, 12
Дублікат          | БЗ         | Колонка E    | 3      | «Петров П.П.» — рядки: 8, 15, 22
Дублікат          | 3бСпП БЗ   | Колонка E    | 2      | «Сидоров С.С.» — рядки: 3, 7
```

## 🎨 UI зміни

### Налаштування обробки
Додано новий чекбокс:
- ✅ Slice_Check
- ✅ Mismatches
- ✅ Sanitizer
- ✅ 3БСП
- ✅ **Дублі** ← NEW!
- ✅ Автовідкриття

## 🚀 Продуктивність

- **Швидкість**: Перевірка дублікатів працює швидко завдяки нормалізації тексту
- **Пам'ять**: Ефективне використання словників для індексації
- **Точність**: Регістронезалежне порівняння з видаленням пробілів

## 📝 Документація

### Для розробників
- Додано коментарі в `excel_duplicates.py`
- Оновлено структуру виконання в `excel_processor.py`
- Документовано формат повернення результатів

### Для користувачів
- Чекбокс "Дублі" дозволяє увімкнути/вимкнути перевірку
- Результати відображаються в листі LOG
- Детальна інформація про кожен дублікат

## ⚙️ Сумісність

- Python 3.11+
- openpyxl 3.1.2
- Electron (поточна версія)
- Windows 10/11

## 🔄 Оновлення з попередніх версій

### З версії 1.5.6
- Просто встановіть нову версію
- Налаштування збережуться автоматично
- Новий чекбокс "Дублі" з'явиться в UI

## 🐞 Відомі обмеження

- Перевірка дублікатів працює тільки для колонок D (ЗС) та E (БЗ, 3бСпП БЗ)
- Порівняння регістронезалежне (Іванов = іванов)
- Порожні клітинки ігноруються

## 📅 Дата релізу

**23 жовтня 2025**

## 👥 Автори

- Розробка: sashashostak
- Тестування: Internal QA

---

**Повний changelog**: [v1.5.6...v1.5.7](https://github.com/sashashostak/KontrNahryuk/compare/v1.5.6...v1.5.7)
