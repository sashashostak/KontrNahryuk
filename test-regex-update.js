// Тест оновленого регексу для підтримки "розпорядженні" ТА "розпорядження"

const testRegex = /розпоряд(женн[іїя]|ження)/i;

const testCases = [
  // Існуючі варіанти (повинні працювати)
  "у розпорядженні командира частини",
  "що перебуває у розпорядженні",  
  "зарахувати у розпорядження",
  "перебувати у розпорядженнi",
  "у розпорядженнi командира",
  
  // Нові варіанти (тепер також повинні працювати)
  "зарахувати у розпорядження командира",
  "перевести у розпорядження частини", 
  "направити у розпорядження",
  "у розпорядження керівника",
  "для розпорядження командуванням",
  
  // Негативні тести (НЕ повинні працювати)
  "розпоряджатися коштами",
  "розпорядник майна",
  "розпорядчий документ",
  "не має розпоряджень"
];

console.log('🧪 Тестування оновленого regex: /розпоряд(женн[іїя]|ження)/i\n');

testCases.forEach((testCase, index) => {
  const matches = testRegex.test(testCase);
  const status = matches ? '✅' : '❌';
  console.log(`${status} Тест ${index + 1}: "${testCase}" → ${matches}`);
});

console.log('\n📊 Підсумок:');
console.log('✅ Повинні знаходитися: розпорядженні, розпорядження, розпорядженнi');
console.log('❌ НЕ повинні знаходитися: розпоряджатися, розпорядник, розпорядчий');

// Додатковий тест для перевірки груп
const detailedTests = [
  "у розпорядженні",  // група 1: женн + і
  "у розпорядження",   // група 2: ження
  "у розпорядженнi",  // група 1: женн + i 
  "у розпоряджені"     // не повинно працювати
];

console.log('\n🔍 Детальний аналіз груп:');
detailedTests.forEach((test, i) => {
  const match = test.match(testRegex);
  if (match) {
    console.log(`✅ "${test}" → знайдено: "${match[0]}", група: "${match[1]}"`);
  } else {
    console.log(`❌ "${test}" → не знайдено`);
  }
});